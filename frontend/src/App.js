
function App() {
  return (
    <div className="App">
    <div className="container">
      <h2>To Do List</h2>
      <div className="top">
          <input
            type="text"
            placeholder="What do you need to do ? "
            // value={text}
            // onChange={(e) => setText(e.target.value)}
          />
          </div>
      </div>
      </div>
  );
}

export default App;
